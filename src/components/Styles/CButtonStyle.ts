import {createStyles} from '../../styles';

const styles = createStyles({
  buttonGroupWrapper: {
    display: 'flex',
    flexDirection: 'row',
  },
  buttonCenter: {
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default styles;
